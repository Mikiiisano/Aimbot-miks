-- Script de administración oculto para Blox Fruits

local player = game.Players.LocalPlayer
local userId = player.UserId
local isAdmin = false
local randomDelay = math.random(1, 5)  -- Retraso aleatorio de 1 a 5 segundos

-- Función para ocultar la administración
local function executeAdminFunction()
    if not isAdmin then
        -- Cambiar atributos de manera discreta
        player:SetAttribute("isAdmin", true)
        isAdmin = true
        -- Simulación de una acción de administración
        local success, err = pcall(function()
            -- Colocar aquí el código para ejecutar la acción de admin
        end)
        
        -- Deshabilitar la impresión de mensajes de consola
        if not success then
            -- Si hay un error, manejarlo sin imprimir en consola
            -- print("Error: " .. err)
        end
    end
end

-- Función para iniciar el script de manera segura
local function startScript()
    wait(randomDelay)  -- Esperar un tiempo aleatorio
    executeAdminFunction()
end

-- Ejecutar la función de inicio
pcall(startScript)
